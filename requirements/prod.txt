fastapi
uvicorn[standard]
sqlalchemy
alembic
psycopg2-binary  # For PostgreSQL database connection
python-dotenv
pydantic-settings # For structured settings management
clerk-backend-api
python-multipart
whatstk
emoji
nltk
pandas
httpx
# Vector Database & AI
qdrant-client
# google-generativeai>=0.3.0
google-genai>=0.1.0  # New library
langchain>=0.1.0
langchain-google-genai>=0.0.8

celery[redis,asyncio]==5.5.3
redis==5.0.1
flower==2.0.1  # Celery monitoring UI (optional but recommended)
slowapi==0.1.9  # Rate limiting middleware

razorpay==1.4.2
stripe==7.9.0

asyncpg
greenlet


# Add these to requirements/base.txt or create requirements/logging.txt

# Logging and monitoring
python-json-logger==2.0.7  # Structured JSON logging
sentry-sdk[fastapi]>=2.20.0  # Error tracking and monitoring (latest, supports profiling + logs)

# For production monitoring (optional - choose based on your needs)
# prometheus-client==0.19.0  # Prometheus metrics export
# datadog==0.48.0  # Datadog APM integration
# opentelemetry-api==1.21.0  # OpenTelemetry tracing
# opentelemetry-sdk==1.21.0
# opentelemetry-instrumentation-fastapi==0.42b0

# Note: The logging system works without external dependencies
# but adding these enables advanced monitoring features